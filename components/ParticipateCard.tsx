'use client';

// TODO: Integrate with main page participate functionality
// import { motion } from 'framer-motion';
// import { FaEthereum } from 'react-icons/fa';

// This component will be integrated later - for now it's a placeholder
interface ParticipateCardProps {
  children: React.ReactNode;
}

export default function ParticipateCard({ children }: ParticipateCardProps) {
  return (
    <div>
      {children}
    </div>
  );
}
